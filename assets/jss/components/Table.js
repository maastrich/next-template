import Colors from 'assets/colors'

const Table = {
    profileTableVar: {
        color: Colors.text,
        component: 'th',
        scop: 'row',
    },
    profileTableVal: {
        color: Colors.text,
        align: 'right',

    },
    profileTable: {
        width: '60vw'
    }
}

export default Table;